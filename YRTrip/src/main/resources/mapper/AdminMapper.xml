<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="admin">

<select id="getTinfoChart" resultType="admin" parameterType="admin">
	SELECT TINFO_ID, COUNT(TINFO_ID) AS COUNTTINFO
	FROM TRAVELBOARDT
	GROUP BY TINFO_ID
	ORDER BY COUNTTINFO DESC
</select>
</mapper>