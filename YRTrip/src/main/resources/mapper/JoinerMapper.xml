<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
 "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="joiner">
<select id="getJoinerList" resultType="joiner">
	SELECT	JOINER_ID,
			TO_CHAR(JOINER_DATE,'YY/MM/DD') JOINER_DATE,
			J.USER_ID,
			J.PARTNER_ID,
			JOINER_CONDITION
	FROM	JOINERT J, PARTNERT P
	WHERE	J.PARTNER_ID = P.PARTNER_ID	
</select>
<insert id="insertJoiner" parameterType="joiner">
	INSERT INTO JOINER (
			JOINER_ID,
			JOINER_DATE,    
			USER_ID,    
			PARTNER_ID
			JOINER_CONDITION
			)
	VALUES (
			#{joinerid},
			sysdate,
			#{userid},
			#{partnerid},
			#{joinercondition}
		)
</insert>
<delete id="deleteJoiner" parameterType="int">
	DELETE FROM JOINERT
		  WHERE JINER_ID = #{value}
</delete>
</mapper>